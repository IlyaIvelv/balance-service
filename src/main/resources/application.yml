server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/balance_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml

  jooq:
    sql-dialect: POSTGRES
    transaction:
      execute-with-connection: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

  cache:
    type: redis # Переключится на simple, если Redis недоступен

jwt:
  secret: c3VwZXItc2VjcmV0LWtleS1mb3ItdGVzdC1wdXJwb3Nlcy1vbmx5
  expiration-ms: 3600000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.balanceservice: INFO
    org.springframework.security: WARN
    org.jooq: DEBUG
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"